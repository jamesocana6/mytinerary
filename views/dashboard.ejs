<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>MyTinerary</title>
</head>
<body>
    <%- include("./partials/nav.ejs")%>
    <h1><%= currentUser.username %>'s Trips</h1>
    <ul>
        <% for (let trip of currentUser.trips) { %>
            <li>
                <a href="/trips/<%=trip._id%>"><%= trip.name %></a> <br>
            </li>
        <% } %>
    </ul>
    <a href="/trips/new">Add New Trip</a>
</body>
</html>